<div class="clearfix">
  <div class="col-md-4">
    <h4>Best Buddy</h4>
    <% other = @player.best_buddy %>
    <%= other ? player_link(other) : "Nobody likes you" %>
  </div>
  <div class="col-md-4">
    <h4>Dynamic Duo</h4>
    <% other = @player.dynamic_duo %>
    <%= other ? player_link(other) : "You ain't Batman" %>
  </div>
  <div class="col-md-4">
    <h4>Ball and Chain</h4>
    <% other = @player.ball_and_chain %>
    <%= other ? player_link(other) : "Still a free man" %>
  </div>
</div>
<div class="clearfix">
  <div class="col-md-4">
    <h4>Rival</h4>
    <% other = @player.rival %>
    <%= other ? player_link(other) : "You have no equal" %>
  </div>
  <div class="col-md-4">
    <h4>Punching Bag</h4>
    <% other = @player.punching_bag %>
    <%= other ? player_link(other) : "You ARE the weakest link" %>
  </div>
  <div class="col-md-4">
    <h4>Nemesis</h4>
    <% other = @player.nemesis %>
    <%= other ? player_link(other) : "No haters" %>
  </div>
</div>